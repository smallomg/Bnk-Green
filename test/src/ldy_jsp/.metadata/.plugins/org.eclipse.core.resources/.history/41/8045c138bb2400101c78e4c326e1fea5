package ex11;

import javax.naming.Context;
import javax.naming.InitialContext;


public class DBconnPool {
	Context initCtx = new InitialContext();
	
	public DBconnPool() {
		try {
			Context initCtx=new In
		}
		
		
	}
	
	public void close() {
		try {
			if(conn != null)conn.close();
		}
	}
}
